<%= (action_name == "index" || action_name == "index_contacted_matches" || action_name == "index_incoming_matches") && (@current_user.send(@current_user.type?).saved_matches.find(match.id)) ? "(saved)" : "" %>
<% if action_name == "index" || action_name == "index_saved_matches" %>
	<% if this_match(match) %>
		<% if i_contacted(this_match(match)) %>
			(you have contacted)
		<% else %>
			(contacting you)
		<% end %>
	<% end %>
<% end %>
<% if action_name == "index" || action_name == "index_saved_matches" || action_name == "index_contacted_matches" || action_name == "index_incoming_matches" %>
	<% if this_match(match) %>
		<% if !this_match(match).messages.blank? %>
			(currently in conversation)
		<% end %>
	<% end %>
<% end %>